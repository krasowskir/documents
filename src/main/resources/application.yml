server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/documents
    username: richard
    password: test123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    database: POSTGRESQL